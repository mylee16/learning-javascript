// Get user input using prompt(“Enter your age:”). If user is 18 or older , give feedback:'You are old enough to drive' but if not 18 give another feedback stating to wait for the number of years he neds to turn 18.

// Enter your age: 30
// You are old enough to drive.

// Enter your age:15
// You are left with 3 years to drive.
// let age = parseInt(prompt("Enter your age:", "Number goes here"))
// const minAge = 18

// if (age >= minAge) {
//     alert("You are old enough to drive")
// }
// else {alert(`You need to wait ${minAge-age} more years before you drive`)}

// Compare the values of myAge and yourAge using if … else. Based on the comparison and log the result to console stating who is older (me or you). Use prompt(“Enter your age:”) to get the age as input.

// Enter your age: 30
// You are 5 years older than me.
// If a is greater than b return 'a is greater than b' else 'a is less than b'. Try to implement it in to ways

// const myAge = 34
// let userInputAge = parseInt(prompt("Enter your age: "))

// if (userInputAge > myAge) {
//     alert(`You are ${userInputAge - myAge} years older than me`)
// }
// else if (myAge > userInputAge) {
//     alert(`I am ${myAge - userInputAge} years older than you`)
// }
// else {
//     alert(`We are the same age`)
// }

// // using switch

// const myAge = 34
// let userInputAge = parseInt(prompt("Enter your age: "))
// let isOlder = myAge > userInputAge
// isOlder
//     ? alert(`I am ${myAge - userInputAge} older than you`)
//     : alert(`You are ${userInputAge - myAge} older than me`)
